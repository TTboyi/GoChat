第一部分：需要优化/改进的点（重构TODO）

下面这些就是“下一版一定要做/可以做更好”的点，按模块分组。你可以直接放进一个 TODO.md 或者“下一版设计文档”的《改进项》章节。

1. 协议层（数据包格式、收发流程）

当前问题

请求用了自定义协议 [4字节header_size][header_pb][args_pb]，但是响应没有统一协议，直接把 LoginResponse protobuf丢回去。

客户端和服务端的 recv 都是“一把梭”：调用一次 recv / retrieveAllAsString()，假设拿到的就是完整的一整个报文。现实中 TCP 是流，不保证一条请求/响应一次性读完，也不保证不会把多条包粘在一起。

为什么是问题

真实网络一定会出现“半包/粘包”，你现在的实现到生产必炸。

没有响应头，客户端无法知道应该读多少字节才是完整响应。

重构要求

定义统一的帧格式（请求和响应都用同一个格式），例如：

[4 bytes: header_len][4 bytes: body_len][header_pb][body_pb]


header_pb 里至少要有：

service_name

method_name

body_len（可冗余）

request_id（将来做并发复用连接时要用）

is_response / status_code（可选）

在 server 端和 client 端都写“解包器”（frame decoder）：

维护一个 buffer

如果 buffer 长度 < 8（不够两个长度字段），继续等

否则读出 header_len 和 body_len

如果 buffer 里数据还不够 8 + header_len + body_len，继续等

如果够了，就切一帧出来，解析这一帧，剩余的继续循环解析（可能有多帧粘在一起）

对发送的逻辑，改成先把整帧组好再一次 send()，保证对端容易解析。

这一步是最高优先级。
不做这一块，你没法说它是一个“真的能跑在长连接/并发调用下的 RPC 框架”。

2. KrpcChannel（客户端调用链）

当前问题

每次调用都：

连接服务器

发送

阻塞 recv

关闭连接

没有超时控制

没有错误处理，把所有错误直接 std::cout

为什么是问题

高延迟 & 资源浪费（TCP三次握手每次都来一遍）

如果服务端挂住了，客户端会永远卡死

没法区分“网络错误”和“业务逻辑报错”

重构要求

KrpcController（或类似的 controller）应该支持：

SetTimeout(ms)

Failed() / ErrorText()：网络级错误 or 框架级错误

KrpcChannel::CallMethod 流程应该变成：

查地址（服务发现）

connect（带超时）

send（完整帧）

循环 recv + 解包器直到拿到一帧完整响应或超时

解析响应的 header，看 status_code

如果 status_code != OK，就把错误写进 controller

把 body 反序列化到 response

给 socket 设置 SO_RCVTIMEO / SO_SNDTIMEO 或者用 select/poll 做超时。

可以延后做的优化（写在“未来迭代”里即可）

连接复用 + 简单的连接池（避免每次重连）

一个 TCP 连接里同时发多个请求（需要 request_id 做复用和回应匹配）

这个比较复杂，不是第一版必做，但是你可以在设计文档的“未来演进”里提到它，让人觉得你有方向。

3. KrpcProvider（服务端分发 / 请求处理）

当前问题

直接在 OnMessage 里假设 buffer 就是完整请求帧，没有按协议解包。

request / response 是 new 出来的 protobuf Message，没有释放，存在内存泄漏。

发送响应时直接发 response.SerializeToString()，没有统一响应帧格式（见“协议层”问题）。

controller 在服务端是 nullptr，导致服务端没法往回报“RPC级错误”。

重构要求

在 OnMessage 里不要一次性 retrieveAllAsString()；要把 buffer 当成“流”，循环尝试解析一帧（按你定义的新帧协议）。Muduo 的标准做法是：写一个 onMessage，它往某个 Context（connection 上的状态）里积累 buffer 并解包。

反射分发逻辑保留（这是你最大的亮点），但要加上 try-catch/异常处理或返回码，防止业务崩一整个进程。

request/response 的生命周期：

你可以把它们交给一个 Closure（回调对象），回调里在发送完响应帧之后手动 delete。

不能忘 request 也要释放。

Closure 在发送时不应该直接引用 protobuf message 的内存指针。应该先 SerializeToString() 成 final_frame，然后用 conn->send(final_frame)，然后安全地释放 message。

否则如果 muduo 只是异步地把你的 string 指针放进待发送队列，等你 delete 掉对象以后就悬空了。

给服务端也加一个 ServerController（可以继承同名接口）：

框架在 dispatch 前创建它

传给业务的 Login(...)

业务可以 controller->SetFailed("no auth")

框架在回包时根据 controller 状态决定返回的状态码/错误信息

4. Zookeeper 服务发现

当前问题

Provider 启动时把 /ServiceName/MethodName 节点直接写成 "ip:port"。

假设每个方法只有一个服务实例。

没有处理 ZK session 过期 / 断线重连 / 重新注册。

Consumer 端拿地址时只 GetData 一次，没有容错（拿不到就直接报错）。

为什么是问题

真正可用的注册中心会同时有多个副本（多实例），并且需要在实例挂掉时自动摘除。

当 provider 的 ZooKeeper session 挂了，临时节点会被删掉，服务应该自动重注册，不然客户端拿不到它了。

重构要求

服务端注册结构建议修改为：

/ServiceName/MethodName/<instanceID> 是临时节点
例子：/UserService/Login/10.0.0.7:8000

节点内容可以是额外元数据（比如机房、权重）

客户端拉取时：

列举子节点，拿到所有候选实例

做一个最简单的负载均衡策略（比如随机/轮询）

ZkClient 需要：

监听 session 事件，如果 session 过期，自动重连并重新创建临时节点。

GetData 之外要加“拿不到就报 controller error”的逻辑，而不是直接继续往下跑。

5. 日志系统（KrpcLogger）

当前问题

异步 logger 是有的（好评），但：

每条日志都 fopen -> fputs -> fclose，写放大、性能差。

没有优雅关闭逻辑（m_exit 永远没被安全地设置）。

日志线程永远 while(true)，进程退出时可能丢日志。

没有日志级别过滤（INFO/ERROR 都一样流）。

重构要求

打开文件句柄后复用，不要每条日志都打开关闭。按日期变更时再 rotate。

提供 Logger::Shutdown()：

设置 m_exit = true

notify_all()

线程 flush 剩余队列再 return

给 LOG_INFO / LOG_ERROR 做级别控制，比如全局日志等级，低等级的日志直接丢弃，减少 IO。

把日志写成 YYYY-MM-DD_HH.log 这种更好分割定位问题。

6. 代码结构 / 角色边界

当前问题

main() 里既有框架初始化，又直接 NotifyService，然后 Run()，放在一起还好，但未来多服务时会变乱。

controller、channel、provider、zk、logger、config 都散着放。

重构要求
建议把框架模块明确分层（方便你重写时有清晰的 package/命名空间划分）：

core/

rpc_channel.h/.cc (client stub 调用通道)

rpc_controller.h/.cc

rpc_provider.h/.cc (服务器注册/分发)

rpc_protocol.h/.cc (帧格式 + 编解码)

net/

tcp_client.h/.cc

tcp_server.h/.cc

connection_context.h (半包解码状态)

registry/

zk_client.h/.cc

service_discovery.h/.cc

util/

logger.h/.cc

config.h/.cc

time.h / idgen.h（以后 request_id 可放这里）

example/

user_service_impl.cc (业务实现)

client_demo.cc

server_main.cc

目标是：以后你自己写新服务时，应该只需要：

int main() {
    KrpcApplication::Init(argc, argv); // 读配置、起日志、连ZK
    KrpcProvider provider;
    provider.notifyService(new UserServiceImpl()); // 注册业务类
    provider.run(); // 阻塞
}


业务侧完全不碰协议 / ZK / muduo。

第二部分：从零解释“这个 RPC 框架到底是怎么工作的（重写时的思路指南）”

我们现在假装你打算自己重写一版。我会告诉你每个组件是干嘛的，以及它们怎么串起来。

1. 背景：为什么要有 RPC？

问题场景：

A 进程想调用 B 进程的某个函数，比如 Login(name, pwd)

直接跨进程没法 call 函数指针，所以我们要走网络（TCP）

我们希望业务写的还是这种风格：

UserServiceRpc_Stub stub(channel);
stub.Login(&controller, &request, &response, nullptr);


就像“本地函数调用”。

RPC 框架帮我们做三件事：

把调用意图（调用哪个函数、参数是什么）转成二进制，丢到网络上。

让对面的服务收到后能知道：我要调哪个业务方法，怎么把二进制还原成对象，再去真正执行逻辑。

执行完之后把返回值打包回来。

2. Protobuf 在这里干嘛？

protobuf 做了两件特别关键的事情：

定义消息类型
例如：

message LoginRequest { bytes name = 1; bytes pwd = 2; }
message LoginResponse { ResultCode result = 1; bool success = 2; }


编译后会自动生成 C++ 类，带 SerializeToString() / ParseFromString()。

定义服务接口
例如：

service UserServiceRpc {
  rpc Login(LoginRequest) returns (LoginResponse);
}


protoc 会帮你生成：

抽象类 class UserServiceRpc : public ::google::protobuf::Service { ... }

里面有虚函数 Login(...)

UserServiceRpc_Stub（客户端用的代理类）

这点非常关键：

你不需要自己手动写“我有哪些方法”。protoc 已经基于 .proto 帮你维护了一份“服务 -> 方法”的目录。

3. 客户端调用链（请求是怎么发出去的？）

你在客户端写：

Kuser::LoginRequest req;
req.set_name("zhang san");
req.set_pwd("123456");

Kuser::LoginResponse rsp;

KrpcChannel channel;          // 你自己实现的网络通道
KrpcController controller;    // 控制错误/超时等
UserServiceRpc_Stub stub(&channel);

stub.Login(&controller, &req, &rsp, nullptr);


这一行 stub.Login(...) 实际上做了这些事：

把 req 序列化成字符串 args_str

构造 RpcHeader（你自己的结构）：

service_name = "UserServiceRpc"

method_name = "Login"

args_size = args_str.size()

把 RpcHeader 也序列化成字符串 header_str

组网络包：

[4字节:header_str长度][header_str字节][args_str字节]


找到这个服务在哪台机器运行（ZooKeeper 查询）

建 TCP 连接，发数据

阻塞收数据，反序列化成 LoginResponse，填进 rsp

你可以把 KrpcChannel 理解成“客户端传输层 + 编码器”。

4. 服务端调用链（请求是怎么被执行的？）

服务端这边你会做：

class UserService : public Kuser::UserServiceRpc {
public:
    void Login(controller, request, response, done) override {
        // 业务逻辑，比如查数据库、校验密码...
        response->set_success(true);
        response->mutable_result()->set_errcode(0);
        done->Run();
    }
};

int main(...) {
    KrpcApplication::Init(...);      // 读配置，连接ZK，起日志
    KrpcProvider provider;           // RPC服务提供者
    provider.NotifyService(new UserService()); // 注册业务
    provider.Run();                  // 进去事件循环 (muduo)
}


重点在 KrpcProvider 里。

KrpcProvider 做的事是：

启动一个基于 muduo 的 TCP 服务器，监听某个端口比如 8000。

把自己提供的服务注册到 ZooKeeper，比如：

/UserServiceRpc/Login = "127.0.0.1:8000"（临时节点）
这样客户端就能通过 ZK 知道去哪儿调。

当有客户端连进来并发送数据：

onMessage() 里读到字节流，按协议解析：

先读4字节拿到 header 长度

反序列化 header 结构（知道调用哪个 service/method）

反序列化请求参数到一个 protobuf request 对象

根据 header 里的 service_name / method_name 去找：

对应的 Service* 实例（就是你 new 的 UserService）

对应的 MethodDescriptor

创建空的 response 对象

调用 service->CallMethod(methodDesc, controller, request, response, done)

这个最终会调到你自己 override 的 UserService::Login(...)

业务函数填好 response 之后，执行 done->Run()，框架就把 response 序列化发回客户端。

核心结论：

业务只关心“我拿到 request -> 我填 response”

框架负责“我怎么从字节流还原到 request，并且在最后发回 response”

这就是“RPC 框架本质上在做方法分发和参数编解码”。

5. ZooKeeper（服务注册 / 服务发现）

为什么需要它？

你不想在客户端硬编码“UserService 在 127.0.0.1:8000”。

你希望以后可以多副本部署、动态下线、自动摘除挂掉的节点。

做法（简化版）：

Provider（服务端）启动的时候，往 ZooKeeper 写一个节点：

路径可以是 /UserServiceRpc/Login

值可以是 "127.0.0.1:8000"

节点类型用“临时节点”（EPHEMERAL），这样服务端进程挂了，节点会自动消失

Consumer（客户端）在 KrpcChannel::CallMethod 里，在真正发请求之前，去 ZooKeeper 读这个节点的值，得到 IP+端口，然后连过去发。

更成熟的版本（你下一版可以直接按这个来）：

路径 /UserServiceRpc/Login/10.0.0.12:8000

有多个子节点，每个子节点是一个实例

客户端把子节点列表拿出来，随机或轮询一个，从而实现负载均衡

6. Logger（日志）

为什么要单独搞一个 logger 线程？

因为：

I/O (磁盘写文件) 是慢的，如果你在业务线程/网络线程里直接 fopen/fputs/fclose，会卡住所有网络请求。

正确做法是：业务线程只是把日志 push 进一个无界/有界队列，然后立刻返回继续干活。

后台日志线程慢慢把日志刷进磁盘。

这个你其实已经做出来了，思路完全对，只是还有健壮性要补（句柄复用、优雅关闭等，前面写了）。

你重写时最小可行版本（MVP 方案）

我来给你一版“最小必须要有的”，你可以按这个目标先实现干净版：

协议帧定义（必须）

[4 bytes header_len][4 bytes body_len][header_pb][body_pb]


header_pb 里含 service_name, method_name, request_id（uint64）, err_code（响应时用）

客户端 RpcChannel（必须）

根据 service/method 去 ZooKeeper 查地址

建 TCP

打包帧，send

阻塞读 + 按协议解包

Parse response body 到用户传进来的 response

支持超时

如果失败，controller->Failed("xxx")

服务端 Provider（必须）

muduo TcpServer

每个连接维护一个 buffer，循环解包帧

取出 service_name/method_name

找到对应的 service 实例和 method descriptor

解析 request body 成 protobuf

调用业务方法

拿到 response protobuf，重新打成响应帧并发回

Zookeeper（建议先保留最小版）

Provider 启动时写一个节点 /Service/Method = "ip:port"

Client 每次调前查一次

以后再扩展到多实例 + 负载均衡

日志（可以先留简单版）

单线程 logger + 队列

每条日志入队

日志线程循环取队、写文件到一个持久打开的 FILE*

提供 Shutdown() 主动 flush

这五个点撑起来，你就不是“在抄 AI 的代码”，而是“我自己实现了一个能跑的 RPC 框架核心”。
